1 - detect standarts shell command;

2 - 
	"cd",
	"ls",
	"awk",
	"grep",
	"pipe",
	"echo",
	"pwd",
	"mkdir",
	"rm",
	"cat",
	"sort",
	"head",
	"tail",
	"wc",
	"bc",
	"clear" 



Concepts:
    ◦ Inter process communication using pipes
    ◦ Termcaps

Autorized functions:
    ◦ fork()
    ◦ wait()
    - exec()

Autorized libraries:
    ◦ The entire libc.
    ◦ The readline library (-lreadline).
    ◦ The termcap and/or the ncurses libraries (-ltermcap and/or -lncurses).


Built-ins
    ◦ cd
    ◦ echo
    ◦ exit
    ◦ type
	◦ man
Notes:
    Functions:
        ◦ chdir
        ◦ subdir

To Do:
	-> improve ["color"] command:
		select specific color passing the respective digit for color
		add colors flags, like ["colors -n"], for view the current color digit
		.["command"] to change text printed after this momment
		in ["colors --help], print MACRO with their respective color

	-> create ["snailfetch"] command:
		like a neofetch, only for print actual banner color letter
		.snailfetch improve to show pc details
		.snailfetch improve to show/hide with
["snailfetch show/hide"]
		.snailfetch improve to show specific arch based
Linux distributions logo ascii/unicode.



	-> create a great man description:
		.to snSH overview
		.to snSH commands like:
			.["color"]



	-> improve Makefile
		add make {update-man} command in Makefile, for put the latest version
of man pages in their respective "/usr/man/"
		.add {make build} for man pages, they dont update only with
["make update-man"] if they dont exist previously,
because rm command will not work, need to build initially.



	-> threat with invalid input:
		.like a lot of backspaces



	-> create ["man"] command:
		to snSH
		.to snSH built-ins(commands)


	-> implement flashback system
		.last commands executed, with arrows


	-> start built-ins
		.ls command, first show all of the folder first. pls

	-> command bar
		.implement a command bar
		.change color of command bar
		.change format of command bar

	-> Arch Package
		.turn snailfetch a arch linux package, to be installed